import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // Placeholder — add meaningful widget tests here.
    expect(true, isTrue);
  });
}
